# Change Request — 5 July 2026

## Summary
UI/branding refinements to the public site plus real project names in the
homepage Collection.

## Changes

### 1. Navigation contrast
- The nav bar's scrolled background was the same sage green as the dark
  sections, so it merged into the page. Changed the scrolled background to a
  deep charcoal-green (`rgba(28,38,32,0.96)`) so the nav reads as a distinct
  band, and added a subtle gold hairline under it for separation.
- File: `src/components/ui/Nav.tsx`

### 2. Removed gold italic heading accents (site-wide)
- Removed the yellow/gold *italic* accent styling from headings on every page
  (28 occurrences across 12 files). The accent words are now upright and use
  the same colour as the rest of the heading — cleaner, more standard type.
- Files: About, Career, Contact, Blogs, Projects, ProjectsByStatus,
  ProjectDetail, Velocity, Collection, CTA, ProjectsGuide, Concierge.

### 3. Minimal hero (earlier in this session)
- Removed the "02 / Waterfront Architecture" eyebrow, the "Discover the
  residence" button, and the "02 / 03" slide counter. Headline now full-width.
- File: `src/components/sections/Hero.tsx`

### 4. Homepage Collection — real project names
Replaced the eight placeholder cards with five projects:
1. **Bare & Bespoke Residence** — Sadashiva Nagar
2. **The Living Edit** — Indiranagar
3. **Sense of Space** — Jayanagar
4. **Bellevue Nest** — Cooke Town
5. **Elite Serenity** — Domlur

- Cards **4 (Bellevue Nest)** and **5 (Elite Serenity)** carry a hidden
  "Velociti" marker — present in the markup (a `data-brand="Velociti"`
  attribute and a screen-reader-only "A Velociti residence" label) but not
  visible to the naked eye.
- File: `src/components/sections/Collection.tsx`

## Notes / follow-ups
- The main Projects section (listing + detail pages, backed by the database)
  still uses the original 8 sample projects. If these 5 should become the
  real, browsable projects with detail pages, that's a separate update.
- "Bare & Bespoke Residence" is a method/approach presented here as a project,
  per request.

## Verification
- 11/11 tests pass · TypeScript typecheck clean · production build OK.
