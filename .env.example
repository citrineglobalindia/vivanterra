# ── Public site ──────────────────────────────────────────────
# Contact form endpoint. Default is /api/contact (same-origin Vercel function).
# Override only if you want to send enquiries to a different collector.
# VITE_CONTACT_ENDPOINT=/api/contact

# ── Admin panel (client) ─────────────────────────────────────
# Find these in Supabase → Project Settings → API.
VITE_SUPABASE_URL=https://tivlznrjwtdtjmmfrczo.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxx

# ── /api/contact (server) ────────────────────────────────────
# Same URL as above. Service-role key is a SECRET — never expose to the client.
SUPABASE_URL=https://tivlznrjwtdtjmmfrczo.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...
